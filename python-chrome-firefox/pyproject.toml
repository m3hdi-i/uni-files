[project]
name = "mobiletest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "playwright>=1.52.0",
    "psutil>=7.0.0",
    "pytest-playwright>=0.7.0",
]
